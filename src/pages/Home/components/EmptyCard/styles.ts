import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 48,
  },
  description: {
    fontSize: 14,
    textAlign: 'center',
    lineHeight: 24,
    color: '#C4C4CC',
  },
})
